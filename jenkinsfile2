pipeline {
    agent {
        label 'slave1'
    }
    
        environment{
            APP_NAME = 'instant'
            VERSION = '2.5'
        }
    
    stages {
        stage ('build') {
            steps {
                echo "building ${APP_NAME}"
                echo "version ${VERSION}"
            }
        }
        stage ('test') {
            steps {
                echo "testing ${APP_NAME}"
                echo "done by krupa" 
            }
        }
        stage('deploy') {
            steps {
                echo "deploying and checking of version"
            }
        }
    }
}
